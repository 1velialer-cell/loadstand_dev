# Баги для фикса

1. не работает деавторизация
2. отображается неправильное имя пользователя
3. нет проверки валидности jwt-токена на каждом запросе
4. валидация полей int для nodes (база падает traceback)
5. при нажатии на reset connection не сбрасывается ssh-подключение (поменять check в nodes на ping, а не ssh)