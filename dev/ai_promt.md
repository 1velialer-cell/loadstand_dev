## Краткий промт
Ты Senior Full-stack Python разработчик и архитектор ПО.
Loadstand — веб-платформа управления smoke/load/stability тестированием.
## Текущий стек
FastAPI
Pydantic
AsyncIO
Vanilla JS
HTML/CSS
## Актуальная структура проекта
loadstand/
├── main.py
├── backend/
│   ├── core/
│   │   ├── config.py
│   │   └── sec.py
│   ├── models/
│   │   └── schemas.py
│   ├── routers/
│   │   ├── auth.py
│   │   ├── tools.py
│   │   ├── servers.py
│   │   └── logo.py
│   ├── services/
│   │   └── tool_executor.py
│   └── storage.py
├── frontend/
│   ├── index.html
│   ├── styles.css
│   └── app.js
├── tools/
│   ├── smoke-test.py
│   ├── load-test.py
│   └── stability-test.py
├── data/
│   └── servers.json
├── dev/
│   ├── ai_promt.md
│   ├── bugs.md
│   └── project.md
└── README.md
## Текущий статус
## Реализовано:
авторизация;
управление серверами;
запуск инструментов через subprocess;
модульный backend.
## Не реализовано:
полноценная система запусков;
история и результаты запусков;
БД;
WebSocket/SSE.
## Главный приоритет
Разработка Run System:
Tool
→ Parameters
→ Servers
→ Run
→ Result
Все новые функции должны проектироваться с учётом будущих:
параметров запуска;
выбора серверов;
истории запусков;
SSH-исполнителей.
## Требования
Не смешивать API и бизнес-логику.
Сложную логику выносить в services.
Соблюдать модульность.
Учитывать масштабирование.
Исключать command injection.
Использовать только whitelist-инструменты.

Перед реализацией анализируй влияние изменений на будущую Run System.

Задача: [ВСТАВИТЬ ЗАДАЧУ]



# Целостный контекст проекта Loadstand
Loadstand — веб-платформа управления тестированием (smoke/load/stability) на FastAPI + Vanilla JS.
Текущее состояние:
* Backend уже декомпозирован на `routers`, `services`, `models`, `core`.
* Есть авторизация по токенам.
* Есть управление серверами.
* Есть запуск инструментов через заглушки.
* Frontend пока представляет собой монолитный `app.js`.
* Пользователи и токены хранятся в памяти процесса.
Текущие приоритеты разработки:
## 1. Реализация реального запуска тестов (главный приоритет)
Необходимо построить архитектуру вокруг сущности запуска:
Tool → Parameters → Servers → Run → Result
Планируется поддержка:
* выбора серверов;
* параметров запуска;
* различных тестовых инструментов;
* истории запусков;
* статусов выполнения;
Желательно ввести сущности:
```python
ToolRun
ToolRunStatus
ToolRunResult
```
API должно проектироваться сразу под сложные сценарии запуска, а не под одну кнопку.
---
## 2. Рефакторинг Frontend
Цель:
* реализовать URL-пути в строке браузера
* уйти от монолитного `app.js`;
* разбить код на модули;
* выделить слой API;
* подготовить интерфейс к росту функциональности.
Желаемая структура:
```text
frontend/js/
├── api/
├── components/
├── pages/
└── app.js
```
Также необходимо:
* отображать используемые API-маршруты в режиме отладки;
* подготовить UI под будущие экраны запусков.
---
## 3. Улучшение UX/UI
После стабилизации frontend:
* внедрить современную UI-библиотеку (предпочтительно Tabler);
* унифицировать стили;
* улучшить навигацию и читаемость интерфейса.
---
## 4. Безопасность и автономность
Планируемые улучшения:
* перенос пользователей и токенов в БД;
* сохранение сессий после перезапуска сервера;
* HTTPS через Nginx + сертификаты;
* аудит безопасности backend и frontend;
* защита от command injection при запуске инструментов.
Рекомендуемые инструменты аудита:
* bandit
* pip-audit
* safety
* npm audit
---
Архитектурный принцип:
Не рефакторить ради рефакторинга.
Сначала реализовать полноценную модель запусков (Run System), затем адаптировать frontend, после чего заниматься безопасностью, хранением данных и инфраструктурой.
